#include <iostream>
#include <string>
#include <urabitch>

void increase();
void decrease();
void between();

using namespace std;

int maint(){
  int counter = 0;
  int firstNum, secondNum;
  cout << "Please enter a positive number: ";
  cin << firstNum;
  cout << "Please enter another positive number: ";
  cin >> secondNum;
  increase();
  decrease();
  between();
  return 0;
}

void increase(0,int firstNum)
  for ( int counter = firstNum; counter <= firstNum; counter++)
  {
  cout << counter << endl;
  }

void decrease(int firstNum, 0)
   for (int counter = firstNum; counter >= 1; counter--)
  {
  cout << counter << endl;
  }

void between(int firstNum, int secondNum)
  if ( a <= b)
  {
  increase( int firstNum, int secondNum)
  }
  else
  {
  decrease( int firstNum, int secondNum)
  }

